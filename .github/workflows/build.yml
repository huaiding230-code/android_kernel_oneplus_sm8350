name: Phase 2 - LineageOS Kernel with SukiSU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048

      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          ref: lineage-23.2
          path: kernel_source
          fetch-depth: 1

      - name: Setup Proton-Clang Toolchain
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p toolchain

          git clone \
            --depth=1 \
            https://github.com/huaiding230-code/proton-clang.git \
            toolchain/proton-clang

          echo "${GITHUB_WORKSPACE}/toolchain/proton-clang/bin" >> "${GITHUB_PATH}"

      - name: Integrate SukiSU Builtin
        shell: bash
        run: |
          set -euo pipefail

          cd "${GITHUB_WORKSPACE}/kernel_source"

          echo "========================================"
          echo "Integrating SukiSU Builtin"
          echo "========================================"

          curl -fsSL \
            "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" \
            | bash -s builtin

          if [ ! -d "KernelSU" ]; then
            echo "ERROR: KernelSU directory was not created."
            exit 1
          fi

          if [ ! -f "KernelSU/kernel/Kconfig" ]; then
            echo "ERROR: KernelSU/kernel/Kconfig not found."
            exit 1
          fi

          if [ ! -L "drivers/kernelsu" ]; then
            echo "ERROR: drivers/kernelsu symlink was not created."
            exit 1
          fi

          echo "SukiSU Builtin integration completed."

      - name: Build LineageOS Kernel
        shell: bash
        run: |
          set -euo pipefail

          cd "${GITHUB_WORKSPACE}/kernel_source"

          CLANG_DIR="${GITHUB_WORKSPACE}/toolchain/proton-clang"
          export PATH="${CLANG_DIR}/bin:${PATH}"

          OUT_DIR="${GITHUB_WORKSPACE}/out"
          mkdir -p "${OUT_DIR}"

          DEFCONFIG="vendor/lahaina-qgki_defconfig"

          MAKE_ARGS=(
            ARCH=arm64
            LLVM=1
            LLVM_IAS=1
            CC=clang
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi-
            O="${OUT_DIR}"
          )

          echo "========================================"
          echo "1. 清理输出目录"
          echo "========================================"

          make "${MAKE_ARGS[@]}" mrproper

          echo "========================================"
          echo "2. 加载 Defconfig"
          echo "========================================"

          make "${MAKE_ARGS[@]}" "${DEFCONFIG}"

          CONFIG_FILE="${OUT_DIR}/.config"

          if [ ! -f "${CONFIG_FILE}" ]; then
            echo "ERROR: .config not found."
            exit 1
          fi

          echo "========================================"
          echo "3. 启用 SukiSU Builtin"
          echo "========================================"

          # 你的源码没有 scripts/config，
          # 所以直接修改输出目录中的 .config。

          if grep -q '^CONFIG_KSU=' "${CONFIG_FILE}"; then
            sed -i 's/^CONFIG_KSU=.*/CONFIG_KSU=y/' "${CONFIG_FILE}"
          elif grep -q '^# CONFIG_KSU is not set' "${CONFIG_FILE}"; then
            sed -i 's/^# CONFIG_KSU is not set/CONFIG_KSU=y/' "${CONFIG_FILE}"
          else
            echo 'CONFIG_KSU=y' >> "${CONFIG_FILE}"
          fi

          echo "========================================"
          echo "4. 重新处理内核配置"
          echo "========================================"

          make "${MAKE_ARGS[@]}" olddefconfig

          echo "========================================"
          echo "5. 检查 SukiSU 配置"
          echo "========================================"

          grep -E '^CONFIG_KSU=' "${CONFIG_FILE}" || true

          if ! grep -q '^CONFIG_KSU=y' "${CONFIG_FILE}"; then
            echo "ERROR: CONFIG_KSU=y is missing."
            exit 1
          fi

          echo "SukiSU Builtin configuration passed."

          echo "========================================"
          echo "6. 编译内核"
          echo "========================================"

          make "${MAKE_ARGS[@]}" -j"$(nproc)"


      - name: Pack with AnyKernel3
        shell: bash
        run: |
          set -euo pipefail

          cd "${GITHUB_WORKSPACE}"

          git clone \
            --depth=1 \
            https://github.com/huaiding230-code/AnyKernel3.git \
            AnyKernel3

          cd AnyKernel3

          rm -rf .git README.md
          rm -f *placeholder 2>/dev/null || true

          sed -i -E \
            's/^[[:space:]]*do\.devicecheck=.*/do.devicecheck=0/' \
            anykernel.sh

          sed -i -E \
            's/^[[:space:]]*(BLOCK|block)=.*/BLOCK=\/dev\/block\/bootdevice\/by-name\/boot;/' \
            anykernel.sh

          sed -i -E \
            's/^[[:space:]]*(IS_SLOT_DEVICE|is_slot_device)=.*/IS_SLOT_DEVICE=1;/' \
            anykernel.sh

          echo "========================================"
          echo "Extracting Kernel Image"
          echo "========================================"

          OUT_DIR="${GITHUB_WORKSPACE}/out"

          if [ -f "${OUT_DIR}/arch/arm64/boot/Image.gz-dtb" ]; then
            cp "${OUT_DIR}/arch/arm64/boot/Image.gz-dtb" ./
            echo "[+] Copied Image.gz-dtb"
          elif [ -f "${OUT_DIR}/arch/arm64/boot/Image" ]; then
            cp "${OUT_DIR}/arch/arm64/boot/Image" ./
            echo "[+] Copied Image"
          else
            echo "[-] ERROR: Kernel image not found."
            exit 1
          fi

      - name: Upload Kernel Zip Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Lineage-SukiSU-Builtin-Kernel
          path: AnyKernel3/*
