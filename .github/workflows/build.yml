name: Phase 2 - Integrated SukiSU Ultra + SUSFS Kernel Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048

      # 1. 拉取 LineageOS 内核源码
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          ref: lineage-23.2
          path: kernel_source
          fetch-depth: 1

      # 2. 拉取工具链
      - name: Setup Proton-Clang Toolchain
        run: |
          mkdir -p toolchain
          git clone --depth=1 https://github.com/huaiding230-code/proton-clang.git toolchain/proton-clang
          echo "${GITHUB_WORKSPACE}/toolchain/proton-clang/bin" >> $GITHUB_PATH

      # 3. 完整复刻你 blu_spark-16-custom 的注入逻辑 (完全交由你的 setup.sh 处理)
      - name: Integrate SukiSU Ultra + SUSFS
        run: |
          set -euo pipefail
          cd kernel_source
          echo "========================================"
          echo "1. 克隆个人仓库 SukiSU Ultra 与 susfs4ksu"
          echo "========================================"
          rm -rf /tmp/SukiSU-Ultra /tmp/susfs4ksu
          git clone --depth=1 https://github.com/huaiding230-code/SukiSU-Ultra.git /tmp/SukiSU-Ultra
          git clone --depth=1 https://github.com/huaiding230-code/susfs4ksu.git /tmp/susfs4ksu

          echo "========================================"
          echo "2. 执行你的 setup.sh"
          echo "========================================"
          bash /tmp/SukiSU-Ultra/kernel/setup.sh

          echo "========================================"
          echo "3. 从 susfs4ksu 注入 SUSFS 模块与头文件"
          echo "========================================"
          SUSFS_C=$(find /tmp/susfs4ksu -name "susfs.c" | head -n 1)
          if [ -n "$SUSFS_C" ]; then
            cp "$SUSFS_C" fs/
          else
            echo "[-] 错误: 未找到 susfs.c" && exit 1
          fi

          find /tmp/susfs4ksu -name "susfs*.h" -exec cp {} include/linux/ \;

          if ! grep -q "susfs.o" fs/Makefile; then
            echo "obj-\$(CONFIG_KSU_SUSFS) += susfs.o" >> fs/Makefile
          fi

          if ! grep -q "CONFIG_KSU_SUSFS" fs/Kconfig; then
            echo -e '\nconfig KSU_SUSFS\n\tbool "SUSFS support"\n\tdefault y\n' >> fs/Kconfig
          fi

      # 4. 执行原版编译 (使用 LineageOS 的 lahaina defconfig)
      - name: Build Integrated Kernel
        run: |
          set -euo pipefail
          cd kernel_source
          CLANG_DIR="${GITHUB_WORKSPACE}/toolchain/proton-clang"
          export PATH="${CLANG_DIR}/bin:${PATH}"
          OUT_DIR="${GITHUB_WORKSPACE}/out"
          rm -rf "${OUT_DIR}"
          mkdir -p "${OUT_DIR}"

          if [ -f "net/core/Kconfig" ]; then
            sed -i 's/^config RPS$/config RPS\n\tprompt "Receive Packet Steering"/g' net/core/Kconfig
          fi

          DEFCONFIG_TARGET="vendor/lahaina-qgki_defconfig"

          MAKE_ARGS=(
            ARCH=arm64
            LLVM=1
            LLVM_IAS=1
            CC=clang
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi-
            O="${OUT_DIR}"
          )

          make "${MAKE_ARGS[@]}" "${DEFCONFIG_TARGET}"

          CONFIG_TOOL="scripts/config"
          if [ -f "${CONFIG_TOOL}" ]; then
            chmod +x "${CONFIG_TOOL}"
            # 1. 核心 Root 框架 (SukiSU / KernelSU)
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --enable CONFIG_KSU
          
          # 2. SUSFS 隐藏框架主开关
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --enable CONFIG_KSU_SUSFS
          
          # 3. KSU 隐藏机制
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --enable CONFIG_KSU_SELINUX_HIDE
          
          # 4. SUSFS 隐藏功能 (路径与挂载隐藏)
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --enable CONFIG_KSU_SUSFS_SUS_PATH
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --disable CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --disable CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT

          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --disable CONFIG_ALWAYS_ENFORCE
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --enable CONFIG_SECURITY_SELINUX_DEVELOP

          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --disable CONFIG_LTO
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --disable CONFIG_THINLTO
          "${CONFIG_TOOL}" --file "${OUT_DIR}/.config" --enable COG_LTO_NNFIONE
          fi

          make "${MAKE_ARGS[@]}" olddefconfig
          make "${MAKE_ARGS[@]}" -j$(nproc)

      # 5. 打包 AnyKernel3
      - name: Pack with AnyKernel3
        shell: bash
        run: |
          set -e
          cd "${GITHUB_WORKSPACE}"
          
          git clone --depth=1 https://github.com/huaiding230-code/AnyKernel3.git AnyKernel3
          cd AnyKernel3
          rm -rf .git README.md *placeholder

          # AnyKernel3 核心防砖参数：强制阉割模块打包功能
          sed -i -E 's/^[[:space:]]*do\.devicecheck=.*/do.devicecheck=0/' anykernel.sh
          sed -i -E 's/^[[:space:]]*(BLOCK|block)=.*/BLOCK=\/dev\/block\/bootdevice\/by-name\/boot;/' anykernel.sh
          sed -i -E 's/^[[:space:]]*(IS_SLOT_DEVICE|is_slot_device)=.*/IS_SLOT_DEVICE=1;/' anykernel.sh
          sed -i -E 's/^[[:space:]]*do\.modules=.*/do.modules=0/' anykernel.sh

          # 复制 dtb (如果有编译出来)
          if [ -f "${GITHUB_WORKSPACE}/out/arch/arm64/boot/dtb" ]; then
            cp "${GITHUB_WORKSPACE}/out/arch/arm64/boot/dtb" ./
          fi

          OUT_DIR="${GITHUB_WORKSPACE}/out"
          if [ -f "${OUT_DIR}/arch/arm64/boot/Image.gz-dtb" ]; then
            cp "${OUT_DIR}/arch/arm64/boot/Image.gz-dtb" ./
          elif [ -f "${OUT_DIR}/arch/arm64/boot/Image" ]; then
            cp "${OUT_DIR}/arch/arm64/boot/Image" ./
          else
            echo "[-] 未找到支持的内核镜像！"
            exit 1
          fi

      - name: Upload Kernel Zip Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU-SUSFS-Kernel
          path: AnyKernel3/*
